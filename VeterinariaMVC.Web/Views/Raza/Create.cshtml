@model VeterinariaMVC.Entidades.ViewModels.Raza.RazaEditViewModel
@{
    ViewBag.Title = "Create";
}

<h2>Nuevo</h2>


@using (Html.BeginForm("Create", "Raza", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Raza</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    @Html.LabelFor(model => model.RazaId)
                    @Html.DropDownListFor(model => model.RazaId, new SelectList(Model.TipoDeMascota, "TipoDeMascotaId", "Descripcion"), "Seleccione un Tipo de Mascota", new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.RazaId, "", new { @class = "text-danger" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.Descripcion)
                    @Html.TextBoxFor(model => model.Descripcion, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.Descripcion, "", new { @class = "text-danger" })
                </div>

            </div>
        </div>
        <div class="form-group">
            <input type="submit" value="Guardar" class="btn btn-primary" />
            @Html.ActionLink("Volver", "Index", "", new { @class = "btn btn-success" })
        </div>

    </div>
}

